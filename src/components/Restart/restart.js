import './restart.css';

const Restart = () => {
    return(
        <main>
            <button type="button" className="button__style">Restart</button>
        </main>
    )
};

export default Restart;